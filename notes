----------TO DO
create profile page 
with edit button
@checkout must prompt 'please setup delivery information' if no saved profile data
update UI with updates in profile
live update
Profile --> cancel edit must return initial values
refactor using react form hook

add auto logout prompt




----------DONE
--> remove signup promise chain --> kept don't want to rewrite
render alert after signup
trouble login get userdata error handler
store profile data
--> order start >> login >> must go to checkout instead of '/' --> not possible w/ redirect to "/" in App.js
--> fixed bug (input ref undefined) when login afte logout
remove axios instances --> use root axios only
optimize refresh auth timer --> leave as is 
enhance burger builder error handling --> removed withErrorHandler
make 'something went wrong page' // not needed anymore
use withErrorhandler to handle network errors // not needed anymore
fix patch request
optimized refreshAuth using useEffect & localToken
convert navigation-buttons to link // no need

